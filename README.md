# Shiva Kasaudhan — Portfolio (JavaScript Edition)

Plain JavaScript (no TypeScript) version built with **Vite + React + Tailwind CSS + Framer Motion**.

## Run locally

```bash
npm install
npm run dev
```

Then open http://localhost:8080
